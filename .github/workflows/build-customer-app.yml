name: Build customer_app debug APK

on:
  workflow_dispatch:
  push:
    paths:
      - 'frontend/customer_app/**'
      - '.github/workflows/build-customer-app.yml'
  pull_request:
    paths:
      - 'frontend/customer_app/**'
      - '.github/workflows/build-customer-app.yml'

jobs:
  build-apk:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend/customer_app

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Set up Flutter (stable)
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Cache pub
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('frontend/customer_app/**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('frontend/customer_app/android/gradle/wrapper/gradle-wrapper.properties', 'frontend/customer_app/android/**/build.gradle*', 'frontend/customer_app/android/**/settings.gradle*', 'frontend/customer_app/android/gradle.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Flutter pub get
        run: flutter pub get

      - name: Build debug APK
        run: flutter build apk --debug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: customer_app-debug-apk
          path: frontend/customer_app/build/app/outputs/flutter-apk/app-debug.apk
          if-no-files-found: error